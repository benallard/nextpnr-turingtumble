Ok, so there is that example, and there is the code.
Let's see if we can piy together a kind of doc.

`ctx`: Some kind of context. never mind.

addWire

addPip

addAlias

addBel

addBelInput

addBelOutput

addBelInOut

addGroupBel

addGroupWire

addGroupPip

addGroupGroup